name: Test Build Image

on:
  workflow_dispatch:

  push:
    tags:
      - 'test'

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: |
          echo "${{ GITHUB_EVENT_NAME }}"
          echo "${{ GITHUB_REF_NAME }}" > test.txt
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          name: ${{ github.event.repository.name }}-${{ GITHUB_REF_NAME }}.txt
          files: test.txt
