[Unit]
Description=Persistent /etc/adjtime
Requires=os-overlay.service
After=mnt-overlay.mount os-overlay.service
Before=network.target

[Mount]
What=/mnt/overlay/etc/adjtime
Where=/etc/adjtime
Type=none
Options=bind

[Install]
WantedBy=os-bind.target
