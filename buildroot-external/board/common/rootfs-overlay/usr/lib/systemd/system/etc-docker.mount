[Unit]
Description=Persistent docker configuration
Requires=mnt-overlay.mount
After=mnt-overlay.mount os-overlay.service
Before=network.target

[Mount]
What=/mnt/overlay/etc/docker
Where=/etc/docker
Type=none
Options=bind

[Install]
WantedBy=os-bind.target
