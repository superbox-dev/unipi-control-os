[Unit]
Description=Persistent /etc/group
Requires=os-overlay.service
After=mnt-overlay.mount os-overlay.service
Before=network.target

[Mount]
What=/mnt/overlay/etc/group
Where=/etc/group
Type=none
Options=bind

[Install]
WantedBy=os-bind.target
