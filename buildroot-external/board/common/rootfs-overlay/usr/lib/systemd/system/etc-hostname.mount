[Unit]
Description=Persistent /etc/hostname
Requires=os-overlay.service
After=mnt-overlay.mount os-overlay.service
Before=network-pre.target

[Mount]
What=/mnt/overlay/etc/hostname
Where=/etc/hostname
Type=none
Options=bind

[Install]
WantedBy=os-bind.target
