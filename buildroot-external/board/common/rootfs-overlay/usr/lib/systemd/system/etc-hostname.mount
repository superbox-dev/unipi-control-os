[Unit]
Description=Persistent hostname configuration
Requires=overlay.mount
After=overlay.mount os-overlay.service
Before=network.target

[Mount]
What=/mnt/overlay/etc/hostname
Where=/etc/hostname
Type=none
Options=bind

[Install]
WantedBy=os-bind.target
