[Unit]
Description=Persistent /etc/machine-info
Requires=os-overlay.service
After=mnt-overlay.mount os-overlay.service
Before=network.target

[Mount]
What=/mnt/overlay/etc/machine-info
Where=/etc/machine-info
Type=none
Options=bind

[Install]
WantedBy=os-bind.target
