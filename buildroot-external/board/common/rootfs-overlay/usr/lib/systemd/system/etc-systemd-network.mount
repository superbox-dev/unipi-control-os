[Unit]
Description=Persistent network configuration
Requires=overlay.mount
After=overlay.mount os-overlay.service
Before=network.target

[Mount]
What=/mnt/overlay/etc/systemd/network
Where=/etc/systemd/network
Type=none
Options=bind

[Install]
WantedBy=os-bind.target
