[Unit]
Description=Overlay for /etc
After=var.mount mnt-overlay.mount os-overlay.service

[Mount]
What=overlay
Where=/etc
Type=overlay
Options=lowerdir=/etc,upperdir=/mnt/overlay/etc,workdir=/mnt/overlay/.atomic

[Install]
WantedBy=local-fs.target
