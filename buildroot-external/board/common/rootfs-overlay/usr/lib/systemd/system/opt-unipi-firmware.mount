[Unit]
Description=Persistent /opt/unipi/firmware
Requires=mnt-data.mount
After=mnt-data.mount

[Mount]
What=/mnt/data/usr/local/opt/unipi/firmware
Where=/opt/unipi/firmware
Options=bind

[Install]
WantedBy=os-bind.target
