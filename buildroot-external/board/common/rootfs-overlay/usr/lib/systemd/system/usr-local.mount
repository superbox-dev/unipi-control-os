[Unit]
Description=Persistent /usr/local
Requires=os-data.service
After=mnt-data.mount os-data.service
Before=network.target

[Mount]
What=/mnt/data/usr/local
Where=/usr/local
Options=bind

[Install]
WantedBy=os-bind.target
