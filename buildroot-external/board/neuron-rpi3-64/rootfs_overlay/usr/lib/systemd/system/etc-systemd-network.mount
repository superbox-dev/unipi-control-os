[Unit]
Description=Network persistent configuration
Requires=mnt-overlay.mount
After=mnt-overlay.mount os-overlay.service
Before=network.target

[Mount]
What=/mnt/overlay/etc/systemd/network
Where=/etc/systemd/network
Type=none
Options=bind

[Install]
WantedBy=os-bind.target
