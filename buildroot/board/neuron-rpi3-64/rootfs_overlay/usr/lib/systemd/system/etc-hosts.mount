[Unit]
Description=Hosts persistent configuration
Requires=mnt-overlay.mount
After=mnt-overlay.mount os-overlay-setup.service
Before=network.target

[Mount]
What=/mnt/overlay/etc/hosts
Where=/etc/hosts
Type=none
Options=bind

[Install]
WantedBy=os-overlay-bind.target
